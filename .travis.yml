language: python
compiler:
  - python
before_install:
  - sudo apt-get update -qq
  - sudo apt-get dist-upgrade -qq
  - sudo apt-get install python-pip
  - sudo pip install ghp-import pelican cpp-coveralls --use-mirrors
script:
  - make github
